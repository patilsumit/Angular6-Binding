import { ZodiacpipePipe } from './zodiacpipe.pipe';

describe('ZodiacpipePipe', () => {
  it('create an instance', () => {
    const pipe = new ZodiacpipePipe();
    expect(pipe).toBeTruthy();
  });
});
